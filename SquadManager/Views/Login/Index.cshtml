@model SquadManager.Models.UserViewModel;

    <section>
        <h1>Autenticação</h1>

        <input type="text" placeholder="Username / email" name="email" value="" />
        <input type="text" name="password" placeholder="Password" />
        <a href="#">Esqueceu a senha?</a>

        <button>Login</button>

        <label>Não tem conta?</label>
        <a href="#">Crie aqui</a>
    </section>

